syntax = "proto3";

option java_package = "com.example.mchnai";

service HelloService {
    rpc sayHelloStream(NumMessages) returns (stream StringMessage);
}

message NumMessages {
    int32 count = 1;
}

message StringMessage {
    string message = 1;
}
